## Install Node
* use Homebrew
* brew install node
* brew doctor
* `node -v` and `npm -v` to check the installation

# Install Angular
* follow [official](https://docs.angularjs.org/tutorial/) documentation.